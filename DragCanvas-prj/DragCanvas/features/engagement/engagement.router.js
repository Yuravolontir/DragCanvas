import { Router } from 'express'; import { verifyToken } from '../../middlewares/auth.js'; import { formSubmitLimiter } from '../../middlewares/rateLimit.js'; import * as ctrl from './engagement.ctrl.js'; const router=Router(); router.get('/public/:projectId/:widgetKey',ctrl.getPublic); router.post('/',formSubmitLimiter,ctrl.submit); router.get('/project/:projectId',verifyToken,ctrl.list); router.put('/project/:projectId/:entryId',verifyToken,ctrl.moderate); export default router;
