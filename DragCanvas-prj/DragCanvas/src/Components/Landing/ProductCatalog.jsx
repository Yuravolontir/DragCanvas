import React from 'react'; import { useNode } from '@craftjs/core'; import { ToolbarSection } from './Toolbar/ToolbarSection'; import { ToolbarItem } from './Toolbar/ToolbarItem'; import { groupLines } from '../../utils/elementData.js';
export const ProductCatalog = ({ products, currency, accent }) => { const { connectors: { connect } } = useNode(); const rows = groupLines(products, 4); const bg = accent ? `rgba(${Object.values(accent)})` : '#0060ac'; return <div ref={connect} style={{ display: 'grid', gridTemplateColumns: 'repeat(3,minmax(0,1fr))', gap: 16, width: '100%' }}>{rows.map(([name, description, price, image], i) => <article key={i} style={{ border: '1px solid #ddd', borderRadius: 12, overflow: 'hidden', padding: 16 }}>{image && <img src={image} alt="" style={{ width: '100%', aspectRatio: '4/3', objectFit: 'cover' }}/>}<h3>{name}</h3><p>{description}</p><strong>{price} {String(currency || 'USD').toUpperCase()}</strong><button type="button" style={{ display: 'block', marginTop: 10, background: bg, color: '#fff', border: 0, borderRadius: 8, padding: 10 }}>Add to cart</button></article>)}</div>; };
const Settings = () => <><ToolbarSection title="Products"><ToolbarItem full propKey="products" type="lines" label="Per product: name, description, price, image URL"/><ToolbarItem full propKey="currency" type="text" label="Currency (USD, EUR, ILS)"/></ToolbarSection><ToolbarSection title="Appearance"><ToolbarItem full propKey="accent" type="bg" label="Buttons"/></ToolbarSection></>;
ProductCatalog.craft = { displayName: 'ProductCatalog', props: { products: ['Starter kit','Everything needed to begin','29.00','', 'Workshop','A private one-hour session','79.00',''], currency: 'USD', accent: { r: 0, g: 96, b: 172, a: 1 } }, related: { toolbar: Settings } };
