/**
 * Kept out of Container.jsx so that file exports only its component - mixing a
 * component with other exports defeats React Refresh and forces a full reload
 * on every edit.
 */
export const ContainerDefaultProps = {
  background: '#ffffff',
  padding: 3,
};
