# Copy this file to .env and fill in the real values
# PostgreSQL connection string (Supabase)
DATABASE_URL=postgresql://user:password@host:5432/postgres

# Must match JWT_SECRET of the Node API: this service verifies the tokens it issues
JWT_SECRET=

# Optional: an extra allowed origin for CORS
FRONTEND_URL=
